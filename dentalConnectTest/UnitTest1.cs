using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using dentalConnectDAO.Implementation;
using dentalConnectDAO.Model;

namespace dentalConnectTest
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void Test()
        {
            //Person p = new Person("4544", "jose", "Sauzi", "Sarara", DateTime.Now, 'F', "1234567890", "john.doe@example.com");
            //Customer c = new Customer("423423", "miEmpresa", "social", "miCuadra");

            //CustomerImpl customerImpl = new CustomerImpl();
            //customerImpl.Insertar(p, c);

            //CustomerImpl customerImpl = new CustomerImpl();
            //customerImpl.Get(7);

            //CustomerImpl customerImpl = new CustomerImpl();
            //customerImpl.Delete(new Customer("423423", "miEmpresa", "social", "miCuadra"));
        }
    }
}
